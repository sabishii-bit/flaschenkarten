# Server
PORT=3000
CORS_ORIGIN=https://your-app.pages.dev

# Database
POSTGRES_USER=flaschenkarten
POSTGRES_PASSWORD=changeme
POSTGRES_DB=flaschenkarten

# Constructed automatically in docker-compose; set manually for local dev
DATABASE_URL=postgres://flaschenkarten:changeme@localhost:5432/flaschenkarten

# Admin auth
ADMIN_PASSWORD=changeme
JWT_SECRET=change-this-to-a-long-random-secret
